name: netflix_project
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  cupertino_icons: 1.0.2
  dartz: 0.10.1
  dio: 4.0.4
  flutter:
    sdk: flutter
  flutter_bloc: 8.0.1
  freezed_annotation: 1.1.0
  get_it: 7.2.0
  google_fonts: 2.2.0
  injectable: 1.5.3
  intl: 0.17.0
  json_annotation: 4.4.0
  json_serializable: 6.1.5
  video_player: 2.3.0

dev_dependencies:

  build_runner: 
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter
  freezed: ^1.1.1
  injectable_generator: 1.5.3

flutter:

  uses-material-design: true

  # assets:
  #   - assets/icons/launcher_icon.png # this is the launcher icon of the app
