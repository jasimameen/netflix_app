import 'package:flutter/material.dart';

class DetailsPageSuggetions extends StatelessWidget {
  const DetailsPageSuggetions({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(50),
      child: Center(
        child:
            Text("Suggetions will Appear here \n(Currently Not Implemended)"),
      ),
    );
  }
}
